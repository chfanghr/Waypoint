name: Waypoint
version: '${project.version}'
main: site.urandom.waypoint.Waypoint
api-version: 1.17
prefix: waypoint
authors: [ fanghr ]

commands:
  w:
    description: waypoint
    usage: /w set <name> (<x> <y> <z> <world>?)?|tp? <name>|(remove|rm) <name>|(list|ls)|(last_death|ld)
    permission: site.urandom.waypoint.command